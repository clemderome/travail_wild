maxBonbons entier = 0

Début maxBonbons (réel argent, réel prix)
  Si argent > 0 && prix > 0
    tant que argent >= prix
      maxBonbons <- maxBonbons +1
      argent <- argent - prix
   fin tant que
  fin Si
   retourner maxBonbons
   
Fin maxBonbons
